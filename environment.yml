name: r2r
channels:
  - conda-forge
  - nvidia
  - pytorch
#  - defaults
dependencies:
  - numba
  - numpy
  - pip
  - pybullet
  - python=3.8
  - pytorch
  - pytorch-cuda=11.7
  - tqdm
  - wandb
  - pip:
      - gym
      - pytorch-lightning
      - "ray[rllib]"
